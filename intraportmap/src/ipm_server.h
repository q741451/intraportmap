#ifndef _IPM_SERVER_H
#define _IPM_SERVER_H

// 服务器端（一个）
class ipm_server
{
public:
	// 服务器listen

	// listen结果（读写挂）

	// 主动跑ipm_agent_server，或tunnel

	// 存一堆ipm_agent_server
	// 存一堆ipm_tunnel，方便查找
};

#endif